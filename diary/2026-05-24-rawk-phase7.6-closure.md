# Phase 7.6 closure — Display elimination, fine Phase 7 (2026-05-24)

**Status:** ✅ GREEN. Unico BRIDGE residuo `PHASE7.2→7.6` rimosso. Phase 7 completata (lato BRIDGE).

Baseline diffrun: **95 MATCH / 9 DIVERGE / 5 SKIPPED** (stabile rispetto alla baseline reale del commit `e1ecaa2`; la nota "96/8/5" nel diario 7.5 era uno snapshot intermedio di una fase di compile non riprodotto al commit finale).

## Modifica

Rimossa l'unica BRIDGE Phase 7 residua:

```diff
-impl fmt::Display for AwkValue {
-    fn fmt(&self, f: &mut fmt::Formatter<'_>) -> fmt::Result {
-        // PHASE7.2→7.6 BRIDGE: Display è String-only; output byte-esatto userà
-        // `write_all(&val.as_string())` direttamente nel path di output (7.6).
-        write!(f, "{}", String::from_utf8_lossy(&self.as_string()))
-    }
-}
```

L'`impl Display` era l'ultimo punto in cui un `from_utf8_lossy` veniva applicato a un `AwkValue` lungo un percorso non-R3. Phase 7.5 aveva già spostato gli output runtime (`Statement::Print`, `Statement::Printf`, `io::handle_output`) sul path bytes diretto via `write_all(&val.as_string())`, lasciando `Display` senza chiamatori reali sul path AWK. Una scansione preliminare con `rg -n 'AwkValue.*to_string|\{.*AwkValue|format!.*AwkValue|write!.*AwkValue|print!.*AwkValue' src/ tests/` non ha trovato chiamatori vivi del trait.

Verifica empirica: rimosso il blocco, `cargo build --all-targets` ha compilato a 0 errori. Nessun consumer rotto = nessun caller reale. Rimosso anche `use std::fmt;` da `src/types.rs` (era importato solo per il trait `Display` ora eliminato).

## Pre-existing fmt fix (collaterale)

`cargo fmt --check` falliva pre-modifica per una riga troppo lunga in `src/runner/fmt.rs:67` (sfuggita al check di chiusura Phase 7.5). Joinata su una sola linea per allinearsi a rustfmt. Nessuna modifica di comportamento.

## Gate (post-7.6)

- `cargo build --all-targets`: ✅ 0 errori.
- `cargo clippy --all-targets`: ✅ 0 lint (warning di hard-link cache exFAT pre-esistenti, ignorabili).
- `cargo fmt --check`: ✅ pulito.
- `cargo test`: ✅ 38 passed (11 suites).
- `bash scripts/checks.sh`: ✅ 7/7 OK (incluso `check_no_macos_forks` dopo cleanup).
- `cargo run --bin diffrun -- tests/testsuite.xml`: ✅ **95 MATCH / 9 DIVERGE / 5 SKIPPED** (= baseline commit 7.5 `e1ecaa2`, nessuna regressione).

## BRIDGE Phase 7 — stato finale

`rg -c 'PHASE7\.' src/` → **0**.

Zero `// PHASE7.X→Y.Z BRIDGE` residui nei sorgenti. Tutti i 23 BRIDGE introdotti nelle sub-phase 7.1→7.6 sono stati eliminati.

## `from_utf8_lossy` inventory finale (11 chiamate reali + 1 menzione doc)

| File | Calls | Categoria |
|------|-------|-----------|
| `src/bin/diffrun.rs` | 3 | Tool diffrun (fuori scope Phase 7) |
| `src/runner/builtins.rs` | 4 | `strftime` chrono fmt, `system`/`close`/`fflush` (cmd/path domain R3) |
| `src/runner/io.rs` | 1 | filename redirect path (R3) — più 1 menzione in doc comment |
| `src/runner/mod.rs` | 3 | ARGV bootstrap filename + 2 getline redirect path/cmd (R3) |
| `src/types.rs` | 0 | (Display impl eliminata in 7.6) |

Tutte le chiamate residue sono **R3-confined** (path/cmd/strftime) o tool-only (`diffrun`). Nessuna è sul path runtime di un valore AWK runtime.

## Phase 7.7 (acceptance)

I 12 testcase byte-aware in `tests/bytes_array_keys.rs` (5), `tests/bytes_regex_match.rs` (1), `tests/bytes_printf.rs` (6) sono già acceptance end-to-end (`std::process::Command::new(binary)` con stdin bytes + assertion su `Vec<u8>`). Phase 7.7 non aggiunge testcase: i 12 esistenti coprono tutti gli scope previsti dal design doc (`length()` byte, `substr()` byte, regex byte alto, FS byte alto, printf %s/%c byte alto, array keys byte, record byte). Phase 7 si conclude qui (lato BRIDGE+coverage).

## Closing remarks

Phase 7 ha sostituito `String → Vec<u8>` lungo l'intero path runtime dei valori AWK (input → record → fields → AwkValue → array keys → regex → builtins → format → output), preservando `String` per i domini R3 (nomi identificatori, path file/cmd, format string strftime). I 109 XML testcase preesistenti sono passati invariati a ogni commit, i 12 testcase byte-aware additivi sono tutti verdi. La transizione è chiusa.

## Commit

- `<HASH>` phase7.6(green): elimina `impl Display for AwkValue` — ultimo BRIDGE 7.2→7.6 chiuso, Phase 7 byte migration completata.
