# Sub-phase 7.3 — Array keys + runtime vars bytes — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Convertire le chiavi array di `EvalContext.arrays` e i runtime var dedicati `fs` / `convfmt` / `ofmt` da `String` a `Vec<u8>`, rimuovendo i 6 BRIDGE `// PHASE7.2→7.3` introdotti dalla sub-phase 7.2 e completando il passaggio bytes per il livello "storage runtime".

**Architecture:** Migrazione layer-by-layer (pattern 7.1/7.2). Si parte da un test RED additivo che pre-codifica il comportamento byte-aware (chiave array con `0x80..0xFF` e NUL embedded). Si convertono in cascade unico le due aree (storage array + runtime vars), perché entrambe condividono il dominio "stato runtime in `types.rs`": cambiare i tipi di firma propaga compile-error che vengono risolti in un singolo `cargo build` verde. Si verifica la baseline diffrun invariata (95 MATCH / 9 DIVERGE / 5 SKIPPED, con `test_gawk_manual_word_frequency` non-deterministico — vedi `rawk_phase7_state.md`). Closure con diary, plan-doc update e state-memory update.

**Tech Stack:** Rust 2021 edition, `std::collections::HashMap`, `Vec<u8>` come backing store dei byte AWK. Test integrazione via `Command::new(env!("CARGO_BIN_EXE_rawk"))` in `tests/`. Gate di verifica: `bash scripts/checks.sh` (7 check) + `cargo test` + `cargo run --bin diffrun -- tests/testsuite.xml`.

---

## File Structure

File toccati in questa sub-phase (tutti già esistenti — nessuna creazione di moduli nuovi):

| File | Responsabilità | Modifiche 7.3 |
|------|---------------|---------------|
| `src/types.rs` | Definizione `EvalContext`, `get_var/set_var/get_array_var/set_array_var`, init dei runtime var | Cambio tipo `arrays`, `fs`, `convfmt`, `ofmt`; rimozione 2 BRIDGE in `get_var`/`set_var`; firma `&str → &[u8]` per `get_array_var/set_array_var`; firma `new(fs: &[u8])` |
| `src/runner/mod.rs` | Valutazione AST, ciclo record, esecuzione statement; ospita helper `eval_array_key` | Return type `eval_array_key: String → Vec<u8>`; adatta 5 call site ArrayAccess/PreInc/PostInc/PreDec/PostDec, AssignArray, Delete; rimuove BRIDGE in `BinaryOperator::In` e `Statement::ForIn`; bootstrap ARGV/ENVIRON via `&[u8]`; `EvalContext::new(fs)` call site adattato |
| `src/runner/builtins.rs` | Built-in AWK (split, sub, gsub, length, …) | Rimuove BRIDGE in `sub`/`gsub` `ArrayAccess`; `set_array_var(..&[u8])` call site; split chiave ARGV→`&i.to_string().as_bytes()` (manteniamo ASCII numeric key) |
| `src/cli.rs` (read-only verifica) | Parsing CLI `-F` | Verificare che `-F <fs>` venga passato come `&[u8]` o `&str` a `EvalContext::new` (probabile adapter 1 riga) |
| `tests/bytes_array_keys.rs` | **NUOVO** — test integrazione byte-aware additivo | 3 testcase: (a) chiave `0x80..0xFF` round-trip, (b) chiave `NUL` embedded, (c) `for-in` iterazione su chiavi byte-arbitrari |
| `diary/2026-05-20-rawk-phase7-bytes-plan.md` | Plan principale (master di Phase 7) | Aggiunta checkbox closure 7.3 nella tabella riassuntiva |
| `diary/2026-05-23-rawk-phase7.3-bytes-plan.md` | (questo file) | n/a — viene committato nel task 7.3.0 |

**File esclusi (intenzionalmente fuori scope 7.3)** — restano con BRIDGE residui per le sub-phase 7.4–7.6:
- Regex matching (`compile_or_get_regex`, `~`/`!~`, `match`, `split` regex, `sub`/`gsub` con pattern) → resta in 7.4.
- Field splitting in `update_record` → resta in 7.5.
- Builtin printf/`%s`/`%c` → resta in 7.5.
- Output handling (`io::handle_output`) → resta in 7.6.

---

## BRIDGE da rimuovere in questa sub-phase

Conteggio iniziale: 6 marker `// PHASE7.2→7.3`.

| # | File | Riga | Contesto |
|---|------|------|----------|
| 1 | `src/types.rs` | 368 | `get_var` — `FS`/`CONVFMT`/`OFMT` ritornati con `into_bytes()` da `String` |
| 2 | `src/types.rs` | 408 | `set_var` — `FS`/`CONVFMT`/`OFMT` ricevuti con `from_utf8_lossy().into_owned()` |
| 3 | `src/runner/mod.rs` | 424 | `eval_array_key` ritorna `String` lossy |
| 4 | `src/runner/mod.rs` | 690 | `BinaryOperator::In` — chiave forzata a `String` |
| 5 | `src/runner/mod.rs` | 785 | `Statement::ForIn` — assegnazione `key.into_bytes()` da `String` |
| 6 | `src/runner/builtins.rs` | 349 | `sub`/`gsub` su `ArrayAccess` — join lossy a `String` |

**Target finale 7.3**: `grep -rn 'PHASE7\.2→7\.3' src/` → 0 risultati.

---

## Pre-flight rituale (eseguire prima di ogni Task)

```bash
cd "/Volumes/Extreme Pro/Claude/testag-awk/rawk"
# Cleanup macOS forks (memoria: macos_forks_cleanup.md)
rtk proxy find . -name '._*' -not -path './target/*' -not -path './.git/*' -delete
# Verifica baseline verde prima di toccare nulla
bash scripts/checks.sh   # 7/7 OK atteso
cargo test 2>&1 | tail -5  # 23 passed / 8 suites atteso
git status --short  # deve essere clean
```

---

## Task 7.3.0 — Pre-flight + plan checkpoint

**Files:**
- Create: `diary/2026-05-23-rawk-phase7.3-bytes-plan.md` (questo file, già scritto da writing-plans)

- [ ] **Step 1: Eseguire pre-flight rituale (vedi sopra)**

Run:
```bash
cd "/Volumes/Extreme Pro/Claude/testag-awk/rawk"
rtk proxy find . -name '._*' -not -path './target/*' -not -path './.git/*' -delete
bash scripts/checks.sh
cargo test 2>&1 | tail -5
git status --short
```
Expected: checks 7/7 OK, `test result: ok. 23 passed`, lavoro pulito tranne il file plan appena creato.

- [ ] **Step 2: Verificare conteggio BRIDGE iniziale**

Run:
```bash
grep -rn 'PHASE7\.2→7\.3' src/ | wc -l | tr -d ' '
```
Expected: `6`

- [ ] **Step 3: Verificare baseline diffrun (opzionale ma raccomandato)**

Run:
```bash
cargo run --bin diffrun -- tests/testsuite.xml 2>&1 | grep -E '^(MATCH|DIVERGE|SKIP|=)' | tail -5
```
Expected: 95 MATCH / 9 DIVERGE / 5 SKIPPED (±1 sul MATCH per non-determinismo `test_gawk_manual_word_frequency`).

- [ ] **Step 4: Commit del plan**

```bash
git add diary/2026-05-23-rawk-phase7.3-bytes-plan.md
git commit -m "phase7.3(plan): writing-plans output — sub-phase 7.3 array keys + runtime vars"
```

---

## Task 7.3.1 — RED test additivo `tests/bytes_array_keys.rs`

**Razionale:** prima di toccare il codice, codifichiamo il comportamento target (chiave array byte-arbitraria) come test RED che fallisce ora e diventa GREEN dopo 7.3.2. Pattern TDD-strict allineato al diary 7.1/7.2.

**Files:**
- Create: `tests/bytes_array_keys.rs`

- [ ] **Step 1: Scrivere il test che deve FALLIRE ora**

Crea `tests/bytes_array_keys.rs` con questo contenuto integrale:

```rust
//! Phase 7.3 acceptance — chiavi array byte-aware.
//!
//! RED prima della migrazione 7.3 (le chiavi `0x80..0xFF` collidono dopo
//! `String::from_utf8_lossy` perché tutti i byte invalidi diventano `\u{FFFD}`).
//! GREEN dopo 7.3.2 (storage `HashMap<Vec<u8>, AwkValue>` byte-pulito).

use std::io::Write;
use std::process::{Command, Stdio};

const RAWK: &str = env!("CARGO_BIN_EXE_rawk");

fn run_with_stdin(prog: &str, stdin: &[u8]) -> Vec<u8> {
    let mut child = Command::new(RAWK)
        .arg(prog)
        .stdin(Stdio::piped())
        .stdout(Stdio::piped())
        .spawn()
        .expect("spawn rawk");
    child
        .stdin
        .as_mut()
        .expect("stdin")
        .write_all(stdin)
        .expect("write stdin");
    let out = child.wait_with_output().expect("wait rawk");
    out.stdout
}

/// Due chiavi che differiscono solo per byte > 0x7f devono restare distinte
/// (con lossy collassano entrambe a U+FFFD e il count diventa 1 invece di 2).
#[test]
fn distinct_high_byte_keys_do_not_collide() {
    // Programma AWK: usa le chiavi $1 (byte arbitrari) per contare occorrenze.
    // L'input è 2 righe con byte > 0x7f differenti; ci aspettiamo conta = 2.
    let prog = r#"{ a[$1]++ } END { n = 0; for (k in a) n++; print n }"#;
    let mut input: Vec<u8> = Vec::new();
    input.extend_from_slice(b"\xC3line1\n"); // 0xC3 + "line1"
    input.extend_from_slice(b"\xE9line2\n"); // 0xE9 + "line2"
    let out = run_with_stdin(prog, &input);
    let out_str = String::from_utf8_lossy(&out);
    assert_eq!(
        out_str.trim(),
        "2",
        "chiavi con byte alti diversi devono restare distinte; output ricevuto: {:?}",
        out_str
    );
}

/// Chiave con NUL embedded: deve essere indicizzabile senza troncamenti.
#[test]
fn nul_embedded_key_round_trips() {
    // Costruiamo una chiave "a\0b" via SUBSEP=\0 e leggiamo a[i,j].
    let prog = r#"BEGIN { SUBSEP = "\0"; a["x","y"] = "found"; print a["x","y"] }"#;
    let out = run_with_stdin(prog, b"");
    let out_str = String::from_utf8_lossy(&out);
    assert_eq!(out_str.trim(), "found");
}

/// for-in deve restituire le chiavi byte-arbitrarie senza corromperle:
/// la lunghezza media delle chiavi recuperate deve coincidere con quella
/// di input (con lossy 0xC3/0xE9 vengono trasformati in 3 byte UTF-8 ciascuno
/// → assertion fallisce).
#[test]
fn for_in_preserves_byte_keys_length() {
    let prog = r#"
        BEGIN { a["\xC3"] = 1; a["\xE9"] = 2 }
        END   { for (k in a) print length(k) }
    "#;
    let out = run_with_stdin(prog, b"");
    let out_str = String::from_utf8_lossy(&out);
    let lines: Vec<&str> = out_str.lines().collect();
    assert_eq!(lines.len(), 2, "atteso 2 chiavi distinte, got {:?}", lines);
    for line in &lines {
        assert_eq!(
            line.trim(),
            "1",
            "ogni chiave deve essere lunga 1 byte (POSIX length byte-count); got {:?}",
            line
        );
    }
}
```

- [ ] **Step 2: Eseguire il test e verificare che FALLISCE**

Run:
```bash
cargo test --test bytes_array_keys 2>&1 | tail -20
```
Expected: `test result: FAILED`, almeno 2 dei 3 test FAIL.
**Atteso specifico**:
- `distinct_high_byte_keys_do_not_collide`: stampa `1` (collision via lossy) invece di `2`.
- `nul_embedded_key_round_trips`: comportamento dipende da SUBSEP path (può MATCH o FAIL a seconda di come `as_string()` lossy comprime — documentare comunque l'osservazione).
- `for_in_preserves_byte_keys_length`: stampa `3` (lunghezza UTF-8 di U+FFFD) invece di `1`.

Se passa già al RED, è un segnale: rivedere la logica di test (potrebbe esserci un comportamento già byte-aware non documentato). Non procedere oltre senza chiarire.

- [ ] **Step 3: Commit del test RED**

```bash
git add tests/bytes_array_keys.rs
git commit -m "phase7.3(tests): RED bytes_array_keys — high-byte/NUL/for-in"
```

---

## Task 7.3.2 — Migrazione array keys → `Vec<u8>`

Cambia tipo di `EvalContext.arrays`, firme di `get_array_var`/`set_array_var`, e return type di `eval_array_key`. Rimuove 4 BRIDGE (helper, In, ForIn, sub/gsub).

**Files:**
- Modify: `src/types.rs` (riga 252, 418-429)
- Modify: `src/runner/mod.rs` (righe 61-70, 131-132, 424-432, 571-605, 690-708, 778-786, 880-893)
- Modify: `src/runner/builtins.rs` (righe 293-298, 343-353)

- [ ] **Step 1: Cambiare il tipo del campo `arrays` in `EvalContext`**

In `src/types.rs:252` sostituire:
```rust
    pub(crate) arrays: HashMap<String, HashMap<String, AwkValue>>,
```
con:
```rust
    pub(crate) arrays: HashMap<String, HashMap<Vec<u8>, AwkValue>>,
```

- [ ] **Step 2: Cambiare firme di `get_array_var`/`set_array_var`**

In `src/types.rs:418-429` sostituire il blocco:
```rust
    pub(crate) fn get_array_var(&self, array_name: &str, key: &str) -> AwkValue {
        self.arrays
            .get(array_name)
            .and_then(|arr| arr.get(key))
            .cloned()
            .unwrap_or(AwkValue::Uninitialized)
    }

    pub(crate) fn set_array_var(&mut self, array_name: &str, key: &str, value: AwkValue) {
        let arr = self.arrays.entry(array_name.to_string()).or_default();
        arr.insert(key.to_string(), value);
    }
```
con:
```rust
    pub(crate) fn get_array_var(&self, array_name: &str, key: &[u8]) -> AwkValue {
        self.arrays
            .get(array_name)
            .and_then(|arr| arr.get(key))
            .cloned()
            .unwrap_or(AwkValue::Uninitialized)
    }

    pub(crate) fn set_array_var(&mut self, array_name: &str, key: &[u8], value: AwkValue) {
        let arr = self.arrays.entry(array_name.to_string()).or_default();
        arr.insert(key.to_vec(), value);
    }
```

- [ ] **Step 3: Cambiare return type di `eval_array_key` in `runner/mod.rs`**

In `src/runner/mod.rs:423-432` sostituire l'intera funzione:
```rust
/// Compone la chiave di un array AWK: valuta i sotto-indici e li unisce con SUBSEP.
// PHASE7.2→7.3 BRIDGE: la chiave array resta String fino a 7.3 (poi Vec<u8> byte-aware).
fn eval_array_key(key_exprs: &[Expr], context: &mut EvalContext) -> String {
    let mut parts: Vec<Vec<u8>> = Vec::new();
    for k in key_exprs {
        parts.push(eval_expr(k, context).as_string());
    }
    let subsep = context.get_var("SUBSEP").as_string();
    String::from_utf8_lossy(&parts.join(subsep.as_slice())).into_owned()
}
```
con:
```rust
/// Compone la chiave di un array AWK: valuta i sotto-indici e li unisce con SUBSEP.
/// Phase 7.3: chiave byte-pulita, nessuna conversione lossy.
fn eval_array_key(key_exprs: &[Expr], context: &mut EvalContext) -> Vec<u8> {
    let mut parts: Vec<Vec<u8>> = Vec::new();
    for k in key_exprs {
        parts.push(eval_expr(k, context).as_string());
    }
    let subsep = context.get_var("SUBSEP").as_string();
    parts.join(subsep.as_slice())
}
```

- [ ] **Step 4: Adattare i 5 call site `set_array_var(arr, &key, ...)` in `runner/mod.rs` (PreInc/PostInc/PreDec/PostDec)**

Niente da fare: i 5 call site (`mod.rs:571-605`) già passano `&key` dove `key` è il return di `eval_array_key`. Cambia solo il tipo deducibile (`String` → `Vec<u8>`), il `&key` produce ora `&Vec<u8>` che deref-coerce a `&[u8]` perfettamente. Verifica in step 9.

- [ ] **Step 5: Adattare `BinaryOperator::In` — rimuovere BRIDGE**

In `src/runner/mod.rs:689-708` sostituire il blocco:
```rust
                BinaryOperator::In => {
                    // PHASE7.2→7.3 BRIDGE: chiave array resta String fino a 7.3.
                    let key = String::from_utf8_lossy(&l_val.as_string()).into_owned();
                    let arr_name = if let Expr::Variable(v) = &**rhs {
                        v.clone()
                    } else {
                        "".to_string()
                    };
                    AwkValue::Number(
                        if context
                            .arrays
                            .get(&arr_name)
                            .map(|a| a.contains_key(&key))
                            .unwrap_or(false)
                        {
                            1.0
                        } else {
                            0.0
                        },
                    )
                }
```
con:
```rust
                BinaryOperator::In => {
                    let key = l_val.as_string();
                    let arr_name = if let Expr::Variable(v) = &**rhs {
                        v.clone()
                    } else {
                        "".to_string()
                    };
                    AwkValue::Number(
                        if context
                            .arrays
                            .get(&arr_name)
                            .map(|a| a.contains_key(key.as_slice()))
                            .unwrap_or(false)
                        {
                            1.0
                        } else {
                            0.0
                        },
                    )
                }
```

- [ ] **Step 6: Adattare `Statement::ForIn` — rimuovere BRIDGE**

In `src/runner/mod.rs:777-786` sostituire:
```rust
            Statement::ForIn(key_name, arr_name, block) => {
                let keys: Vec<String> = context
                    .arrays
                    .get(arr_name)
                    .map(|arr| arr.keys().cloned().collect())
                    .unwrap_or_default();

                for key in keys {
                    // PHASE7.2→7.3 BRIDGE: chiave array resta String fino a 7.3.
                    context.set_var(key_name, AwkValue::String(key.into_bytes()));
```
con:
```rust
            Statement::ForIn(key_name, arr_name, block) => {
                let keys: Vec<Vec<u8>> = context
                    .arrays
                    .get(arr_name)
                    .map(|arr| arr.keys().cloned().collect())
                    .unwrap_or_default();

                for key in keys {
                    context.set_var(key_name, AwkValue::String(key));
```

- [ ] **Step 7: Adattare bootstrap ARGV/ENVIRON in `runner/mod.rs:61-70`**

In `src/runner/mod.rs:61-70` sostituire:
```rust
    context.set_array_var("ARGV", "0", AwkValue::from_str_num(b"rawk".to_vec()));
    for (i, file) in config.input_files.iter().enumerate() {
        context.set_array_var(
            "ARGV",
            &format!("{}", i + 1),
            AwkValue::from_str_num(file.clone().into_bytes()),
        );
    }
    for (key, val) in std::env::vars() {
        context.set_array_var("ENVIRON", &key, AwkValue::from_str_num(val.into_bytes()));
    }
```
con:
```rust
    context.set_array_var("ARGV", b"0", AwkValue::from_str_num(b"rawk".to_vec()));
    for (i, file) in config.input_files.iter().enumerate() {
        let key_str = format!("{}", i + 1);
        context.set_array_var(
            "ARGV",
            key_str.as_bytes(),
            AwkValue::from_str_num(file.clone().into_bytes()),
        );
    }
    for (key, val) in std::env::vars() {
        context.set_array_var(
            "ENVIRON",
            key.as_bytes(),
            AwkValue::from_str_num(val.into_bytes()),
        );
    }
```

- [ ] **Step 8: Adattare lookup ARGV in `runner/mod.rs:131-132`**

In `src/runner/mod.rs:131-132` sostituire:
```rust
        if let Some(arr) = context.arrays.get("ARGV")
            && let Some(val) = arr.get(&i.to_string())
        {
```
con:
```rust
        if let Some(arr) = context.arrays.get("ARGV")
            && let Some(val) = arr.get(i.to_string().as_bytes())
        {
```

- [ ] **Step 9: Adattare `sub`/`gsub` su `ArrayAccess` in `builtins.rs:343-353` — rimuovere BRIDGE**

In `src/runner/builtins.rs:343-353` sostituire:
```rust
                    Expr::ArrayAccess(arr, ks) => {
                        let mut keys: Vec<Vec<u8>> = Vec::new();
                        for k in ks {
                            keys.push(eval_expr(k, context).as_string());
                        }
                        let subsep = context.get_var("SUBSEP").as_string();
                        // PHASE7.2→7.3 BRIDGE: array key resta String fino a 7.3.
                        let key =
                            String::from_utf8_lossy(&keys.join(subsep.as_slice())).into_owned();
                        context.set_array_var(arr, &key, AwkValue::String(new_val.into_bytes()));
                    }
```
con:
```rust
                    Expr::ArrayAccess(arr, ks) => {
                        let mut keys: Vec<Vec<u8>> = Vec::new();
                        for k in ks {
                            keys.push(eval_expr(k, context).as_string());
                        }
                        let subsep = context.get_var("SUBSEP").as_string();
                        let key = keys.join(subsep.as_slice());
                        context.set_array_var(arr, &key, AwkValue::String(new_val.into_bytes()));
                    }
```

- [ ] **Step 10: Adattare `split` builtin call site in `builtins.rs:293-301`**

In `src/runner/builtins.rs` cerca il blocco `for (i, p) in parts.iter().enumerate()` (~riga 294). La chiave numerica viene generata da `format!("{}", i + 1)`. Sostituire:
```rust
                let key = format!("{}", i + 1);
                context.set_array_var(
                    &arr_name,
                    &key,
                    AwkValue::from_str_num(p.as_bytes().to_vec()),
```
con:
```rust
                let key = format!("{}", i + 1);
                context.set_array_var(
                    &arr_name,
                    key.as_bytes(),
                    AwkValue::from_str_num(p.as_bytes().to_vec()),
```

- [ ] **Step 11: Compile cascade — `cargo build --all-targets`**

Run:
```bash
cargo build --all-targets 2>&1 | tail -40
```
Expected: 0 errori. Se compaiono errori residui in `runner/mod.rs:880-893` (`Delete`, `AssignArray`) sui call site `&key`, è perché `&Vec<u8>` non viene auto-deref dove la signature richiede `&[u8]` — basta cambiare `&key` in `&key` resta valido (deref-coerce funziona). Se permane: cambiare in `key.as_slice()` puntualmente.

Pattern di fix tipici:
- `arr.remove(&key)` → resta uguale (deref-coerce su `HashMap<Vec<u8>, _>::remove`).
- `set_array_var(arr_name, &key, val)` → resta uguale.
- Eventuali `&key.to_string()` da rimuovere se trovati.

- [ ] **Step 12: Run dei test specifici 7.3 (devono passare ora)**

Run:
```bash
cargo test --test bytes_array_keys 2>&1 | tail -10
```
Expected: `test result: ok. 3 passed; 0 failed`.

- [ ] **Step 13: Gate completo — `bash scripts/checks.sh`**

Run:
```bash
rtk proxy find . -name '._*' -not -path './target/*' -not -path './.git/*' -delete
bash scripts/checks.sh
```
Expected: tutti i 7 check PASS.

- [ ] **Step 14: Verifica regression XML — `cargo test --test xml_runner_test`**

Run:
```bash
cargo test --test xml_runner_test 2>&1 | tail -5
```
Expected: `test result: ok` con 0 failures.

- [ ] **Step 15: Verifica diffrun invariato**

Run:
```bash
cargo run --bin diffrun -- tests/testsuite.xml 2>&1 | grep -E '^=' | tail -5
```
Expected: MATCH ≥ 95 (±1 per non-determinismo word_frequency), DIVERGE = 9, SKIP = 5.
**Se DIVERGE > 9: regressione, fermarsi e investigare.**

- [ ] **Step 16: Verifica conteggio BRIDGE intermedio**

Run:
```bash
grep -rn 'PHASE7\.2→7\.3' src/ | wc -l | tr -d ' '
```
Expected: `2` (restano solo i 2 BRIDGE in `types.rs:368` e `types.rs:408` — quelli per `fs`/`convfmt`/`ofmt`, target del Task 7.3.3).

- [ ] **Step 17: Commit della migrazione array keys**

```bash
git add src/types.rs src/runner/mod.rs src/runner/builtins.rs
git commit -m "phase7.3(types,runner,builtins): array keys diventano Vec<u8>"
```

---

## Task 7.3.3 — Migrazione runtime vars `fs`/`convfmt`/`ofmt` → `Vec<u8>`

Cambia tipo dei 3 campi `EvalContext.fs/convfmt/ofmt`, init, `EvalContext::new`, e i 2 BRIDGE in `get_var`/`set_var`. Adatta `update_record` (per ora resta BRIDGE 7.2→7.5: il field split su `&str` non cambia comportamento — bisogna solo convertire `&self.fs` al posto giusto).

**Files:**
- Modify: `src/types.rs` (righe 248-289, 306-323, 367-371, 407-411)
- Modify: `src/runner/mod.rs` (riga 55 — call site `EvalContext::new`)
- Modify: `src/runner/builtins.rs` (~riga 289, `split` usa `context.fs.clone()`)
- Modify: `src/cli.rs` (riga TBD — verifica come `-F` viene passato a `EvalContext::new`)

- [ ] **Step 1: Cambiare i 3 campi nel struct `EvalContext`**

In `src/types.rs:248` sostituire:
```rust
    pub(crate) fs: String,
```
con:
```rust
    pub(crate) fs: Vec<u8>,
```

In `src/types.rs:259-260` sostituire:
```rust
    pub(crate) convfmt: String,
    pub(crate) ofmt: String,
```
con:
```rust
    pub(crate) convfmt: Vec<u8>,
    pub(crate) ofmt: Vec<u8>,
```

- [ ] **Step 2: Cambiare firma di `EvalContext::new`**

In `src/types.rs:266` sostituire:
```rust
    pub(crate) fn new(fs: &str) -> Self {
```
con:
```rust
    pub(crate) fn new(fs: &[u8]) -> Self {
```

In `src/types.rs:273` sostituire:
```rust
            fs: fs.to_string(),
```
con:
```rust
            fs: fs.to_vec(),
```

In `src/types.rs:284-285` sostituire:
```rust
            convfmt: "%.6g".to_string(),
            ofmt: "%.6g".to_string(),
```
con:
```rust
            convfmt: b"%.6g".to_vec(),
            ofmt: b"%.6g".to_vec(),
```

- [ ] **Step 3: Adattare `update_record` per `fs: Vec<u8>` (BRIDGE 7.2→7.5 resta)**

In `src/types.rs:306-323` il blocco usa `self.fs == " "` e `line.split(&self.fs)`. Sostituire:
```rust
        // PHASE7.2→7.5 BRIDGE: field splitting opera ancora su str; 7.5 porta
        // lo split byte-aware (FS byte-arbitrario).
        let line_str = String::from_utf8_lossy(line);
        let line = line_str.as_ref();

        // Awk default field splitting: split by whitespace, and ignore leading/trailing whitespace
        if self.fs == " " {
            self.fields = line
                .split_whitespace()
                .map(|s| AwkValue::from_str_num(s.as_bytes().to_vec()))
                .collect();
        } else {
            // Split by custom Field Separator
            self.fields = line
                .split(&self.fs)
                .map(|s| AwkValue::from_str_num(s.as_bytes().to_vec()))
                .collect();
        }
```
con:
```rust
        // PHASE7.2→7.5 BRIDGE: field splitting opera ancora su str; 7.5 porta
        // lo split byte-aware (FS byte-arbitrario).
        let line_str = String::from_utf8_lossy(line);
        let line = line_str.as_ref();
        let fs_lossy = String::from_utf8_lossy(&self.fs);

        // Awk default field splitting: split by whitespace, and ignore leading/trailing whitespace
        if fs_lossy.as_ref() == " " {
            self.fields = line
                .split_whitespace()
                .map(|s| AwkValue::from_str_num(s.as_bytes().to_vec()))
                .collect();
        } else {
            // Split by custom Field Separator
            self.fields = line
                .split(fs_lossy.as_ref())
                .map(|s| AwkValue::from_str_num(s.as_bytes().to_vec()))
                .collect();
        }
```

- [ ] **Step 4: Rimuovere il BRIDGE in `get_var` (`types.rs:367-371`)**

In `src/types.rs:367-371` sostituire:
```rust
            // PHASE7.2→7.3 BRIDGE: fs/convfmt/ofmt restano String fino a 7.3.
            "FS" => return AwkValue::String(self.fs.clone().into_bytes()),
            "CONVFMT" => return AwkValue::String(self.convfmt.clone().into_bytes()),
            "OFMT" => return AwkValue::String(self.ofmt.clone().into_bytes()),
```
con:
```rust
            "FS" => return AwkValue::String(self.fs.clone()),
            "CONVFMT" => return AwkValue::String(self.convfmt.clone()),
            "OFMT" => return AwkValue::String(self.ofmt.clone()),
```

- [ ] **Step 5: Rimuovere il BRIDGE in `set_var` (`types.rs:407-411`)**

In `src/types.rs:407-411` sostituire:
```rust
            // PHASE7.2→7.3 BRIDGE: fs/convfmt/ofmt restano String fino a 7.3.
            "FS" => self.fs = String::from_utf8_lossy(&value.as_string()).into_owned(),
            "CONVFMT" => self.convfmt = String::from_utf8_lossy(&value.as_string()).into_owned(),
            "OFMT" => self.ofmt = String::from_utf8_lossy(&value.as_string()).into_owned(),
```
con:
```rust
            "FS" => self.fs = value.as_string(),
            "CONVFMT" => self.convfmt = value.as_string(),
            "OFMT" => self.ofmt = value.as_string(),
```

- [ ] **Step 6: Adattare il call site di `EvalContext::new` in `runner/mod.rs:55`**

Leggi il contesto:
```bash
grep -n 'EvalContext::new' src/runner/mod.rs
```
Trovi (~riga 55): `let mut context = EvalContext::new(fs);` dove `fs: &str` proviene dalla firma di `run(...)`. Due opzioni:

**Opzione A (raccomandata)**: cambiare la firma di `run`/`runner_entry` per accettare `fs: &[u8]`. Più pulito ma propaga il refactor a `cli.rs`/`main.rs`.

**Opzione B (minimal, BRIDGE temporaneo accettabile)**: convertire `fs.as_bytes()` al call site. Adatto se `cli.rs` continua a parsare `-F` come `String` (UTF-8).

Scegliere B se la firma `run` è esposta a molti caller; A se è interna. Verificare con:
```bash
grep -n 'fn run\b\|fn execute' src/runner/mod.rs | head -5
```

Applicare **Opzione B** se in dubbio:
```rust
let mut context = EvalContext::new(fs.as_bytes());
```

- [ ] **Step 7: Adattare `split` builtin che usa `context.fs.clone()` in `builtins.rs:~289`**

Verifica il codice attuale:
```bash
grep -n 'context.fs' src/runner/builtins.rs
```
Trovi (~riga 289):
```rust
            } else {
                context.fs.clone()
            };
            let re = context.compile_or_get_regex(&fs);
```
Dato che `compile_or_get_regex` accetta `&str` (BRIDGE 7.2→7.4), sostituire:
```rust
            } else {
                context.fs.clone()
            };
            let re = context.compile_or_get_regex(&fs);
```
con:
```rust
            } else {
                String::from_utf8_lossy(&context.fs).into_owned()
            };
            let re = context.compile_or_get_regex(&fs);
```
**Nota**: questa rimane un BRIDGE 7.2→7.4 (regex pattern lossy), ma adattato al nuovo tipo di `context.fs`. Marcalo con commento se non c'è già un BRIDGE marker nelle vicinanze.

- [ ] **Step 8: Verifica eventuali altri usi di `context.fs`/`context.convfmt`/`context.ofmt` come `&str`**

Run:
```bash
grep -rn 'context\.fs\|context\.convfmt\|context\.ofmt\|\.convfmt\b\|\.ofmt\b' src/ | head -20
```
Per ogni hit non-banale: assicurati che il chiamante accetti `Vec<u8>` o esegui conversione `String::from_utf8_lossy(&...).into_owned()` con marker BRIDGE se serve.

Punto di attenzione noto: `runner/mod.rs:443` `let convfmt = context.convfmt.clone();` poi `as_string_convfmt(&convfmt)` — la signature di `as_string_convfmt` è `fn(&self, fmt: &str) -> Vec<u8>`. Bisogna mantenerla come `&str` (è formato printf) oppure cambiare la firma. Soluzione minimale:
```rust
            let convfmt = String::from_utf8_lossy(&context.convfmt).into_owned();
```
Oppure cambiare la firma di `as_string_convfmt(&self, fmt: &[u8])` e convertire lossy dentro (BRIDGE 7.2→7.5). Decisione: **prima opzione (lossy al call site)** — meno propagazione, e printf format string sarà comunque migrato in 7.5.

- [ ] **Step 9: Compile cascade — `cargo build --all-targets`**

Run:
```bash
cargo build --all-targets 2>&1 | tail -40
```
Expected: 0 errori. Errori tipici e fix:
- `expected &str, found &Vec<u8>` su chiamata `compile_or_get_regex(&fs)` → wrap con `&String::from_utf8_lossy(&fs).into_owned()` + marker BRIDGE 7.2→7.4.
- `expected String, found Vec<u8>` su passaggio a fmt/printf → wrap lossy + marker BRIDGE 7.2→7.5.
- Eventuali `format!("{}", context.fs)` → `format!("{}", String::from_utf8_lossy(&context.fs))`.

- [ ] **Step 10: Eseguire suite completa**

Run:
```bash
cargo test 2>&1 | tail -10
```
Expected: `test result: ok` su tutte e 9 le suite (8 esistenti + bytes_array_keys nuova). Totale ≥ 26 passed.

- [ ] **Step 11: Verifica diffrun invariato**

Run:
```bash
cargo run --bin diffrun -- tests/testsuite.xml 2>&1 | grep -E '^=' | tail -5
```
Expected: 95 MATCH ±1 / 9 DIVERGE / 5 SKIPPED.

- [ ] **Step 12: Gate completo `bash scripts/checks.sh`**

Run:
```bash
rtk proxy find . -name '._*' -not -path './target/*' -not -path './.git/*' -delete
bash scripts/checks.sh
```
Expected: 7/7 OK.

- [ ] **Step 13: Verifica conteggio BRIDGE finale 7.3**

Run:
```bash
grep -rn 'PHASE7\.2→7\.3' src/ | wc -l | tr -d ' '
```
Expected: `0`.

Run:
```bash
grep -rc 'from_utf8_lossy' src/
```
Expected: count totale ≤ 42 (giù di almeno 2 rispetto a 44 pre-7.3; ottimale ≤ 40, ma non bloccante — i BRIDGE residui per 7.4/7.5/7.6 contribuiscono).

- [ ] **Step 14: Commit della migrazione runtime vars**

```bash
git add src/types.rs src/runner/mod.rs src/runner/builtins.rs
# Eventuale src/cli.rs se modificato in Opzione A
git commit -m "phase7.3(types,runner): fs/convfmt/ofmt diventano Vec<u8>"
```

---

## Task 7.3.4 — Closure diary + plan update + state memory update

**Files:**
- Create: `diary/2026-05-23-rawk-phase7.3-closure.md`
- Modify: `diary/2026-05-20-rawk-phase7-bytes-plan.md` (aggiornamento tabella riassuntiva)
- Modify: `/Users/francescotinti/.claude/projects/-Volumes-Extreme-Pro-Claude-testag-awk/memory/rawk_phase7_state.md`
- Modify: `/Users/francescotinti/.claude/projects/-Volumes-Extreme-Pro-Claude-testag-awk/memory/MEMORY.md`

- [ ] **Step 1: Scrivere il diary di chiusura 7.3**

Crea `diary/2026-05-23-rawk-phase7.3-closure.md` con:

```markdown
# Sub-phase 7.3 — Closure (array keys + runtime vars bytes)

**Date**: 2026-05-23
**Status**: ✅ DONE

## Cosa è cambiato

- `EvalContext.arrays: HashMap<String, HashMap<Vec<u8>, AwkValue>>`.
- `EvalContext.fs / convfmt / ofmt: Vec<u8>`.
- `EvalContext::new(fs: &[u8])`.
- `get_array_var(name: &str, key: &[u8])`, `set_array_var(name: &str, key: &[u8], val)`.
- `eval_array_key(...) -> Vec<u8>`.
- 6 BRIDGE `// PHASE7.2→7.3` rimossi (`types.rs:368`, `types.rs:408`, `runner/mod.rs:424`, `runner/mod.rs:690`, `runner/mod.rs:785`, `builtins.rs:349`).
- 3 testcase byte-aware additivi in `tests/bytes_array_keys.rs` (chiavi 0x80–0xFF distinte, NUL embedded, for-in length).

## Baseline verificata

- `cargo build --all-targets`: 0 errori, 0 warning.
- `cargo clippy --all-targets -- -D warnings`: 0 lint.
- `cargo fmt --check`: pulito.
- `bash scripts/checks.sh`: 7/7 OK.
- `cargo test`: 26+ passed / 9 suites.
- `diffrun tests/testsuite.xml`: 95 MATCH / 9 DIVERGE / 5 SKIPPED (invariato vs 7.2, ±1 word_frequency non-determinismo).

## BRIDGE residui dopo 7.3

- `// PHASE7.2→7.3`: 0 ✅
- `// PHASE7.2→7.4`: 9 (regex matcher — target 7.4).
- `// PHASE7.2→7.5`: 5 + nuovi adapter introdotti in 7.3.3 (printf %s, split su fs lossy). Target 7.5.
- `// PHASE7.2→7.6`: 2 (output handle). Target 7.6.
- `from_utf8_lossy` count totale src/: TBD (≤ 42, target finale Phase 7: ≤ 3).

## Commit di questa sub-phase

1. `phase7.3(plan): writing-plans output — sub-phase 7.3 array keys + runtime vars`
2. `phase7.3(tests): RED bytes_array_keys — high-byte/NUL/for-in`
3. `phase7.3(types,runner,builtins): array keys diventano Vec<u8>`
4. `phase7.3(types,runner): fs/convfmt/ofmt diventano Vec<u8>`
5. `phase7.3(diary): chiusura — array keys + runtime vars bytes, 95/9/5 invariato`

Stato master: ahead +14 vs origin/master (push opzionale, autorizzazione utente).

## Prossima sub-phase

7.4 — Regex matcher bytes (`regex::bytes` crate). Riferimento: design doc §3 sub-phase 7.4 + plan principale tabella riassuntiva.
```

- [ ] **Step 2: Aggiornare la tabella del plan principale**

In `diary/2026-05-20-rawk-phase7-bytes-plan.md` (~riga 820, tabella sub-phase) marcare 7.3 come DONE. Sostituire la riga 7.3 della tabella:
```markdown
| 7.3 | `src/types.rs` (EvalContext.arrays, fs, convfmt, ofmt), `runner/mod.rs` array access | `// PHASE7.2→7.3 BRIDGE` (array key lossy) |
```
con:
```markdown
| 7.3 ✅ | `src/types.rs` (EvalContext.arrays, fs, convfmt, ofmt), `runner/mod.rs` array access, `builtins.rs` sub/gsub | `// PHASE7.2→7.3 BRIDGE` rimossi (6/6, 2026-05-23) |
```

- [ ] **Step 3: Aggiornare la memory `rawk_phase7_state.md`**

Edit file: `/Users/francescotinti/.claude/projects/-Volumes-Extreme-Pro-Claude-testag-awk/memory/rawk_phase7_state.md`.

Cambi specifici:
- Header `description` → `"Phase 7 di rawk — sub-phase 7.3 array keys + runtime vars ✅ chiusa, prossimo: 7.4 regex matcher bytes."`
- Sezione `## Artefatti committati` → aggiungere blocco 7.3 (5 commit) sotto i 9 esistenti, totale master ahead +14.
- Sezione `## Prossima azione concreta` → riscrivere targeting 7.4 (regex matcher bytes via `regex::bytes`), 9 BRIDGE 7.2→7.4 da rimuovere, files principali `types.rs:regex_cache` + `runner/mod.rs` (operatori match e split regex).
- Sezione BRIDGE residui → update conteggi (target →7.3 = 0, →7.4 invariato 9, →7.5 potenzialmente +1-2 da adapter introdotti in 7.3.3, →7.6 invariato 2).
- `from_utf8_lossy` count aggiornato (eseguire `grep -rc from_utf8_lossy src/` e riportare).

- [ ] **Step 4: Aggiornare MEMORY.md index**

Edit file: `/Users/francescotinti/.claude/projects/-Volumes-Extreme-Pro-Claude-testag-awk/memory/MEMORY.md`.

Cambiare la riga rawk_phase7_state da:
```markdown
- [rawk Phase 7 state](rawk_phase7_state.md) — sub-phase 7.2 AwkValue+AST core ✅ chiusa, 9 commit locali non pushati, 22 BRIDGE 7.2→7.{3,4,5,6}, prossimo: sub-phase 7.3 array keys + runtime vars
```
a:
```markdown
- [rawk Phase 7 state](rawk_phase7_state.md) — sub-phase 7.3 array keys + runtime vars ✅ chiusa, 14 commit locali non pushati, 16 BRIDGE 7.2→7.{4,5,6}, prossimo: sub-phase 7.4 regex matcher bytes
```
(adattare il conteggio "16" al numero effettivo di BRIDGE residui osservato in Step 3 di Task 7.3.3).

- [ ] **Step 5: Commit closure**

```bash
git add diary/2026-05-23-rawk-phase7.3-closure.md diary/2026-05-20-rawk-phase7-bytes-plan.md
git commit -m "phase7.3(diary): chiusura — array keys + runtime vars bytes, 95/9/5 invariato"
```

(Le memory `~/.claude/...` sono fuori repo e non vanno committate.)

- [ ] **Step 6: Gate finale post-closure**

Run:
```bash
rtk proxy find . -name '._*' -not -path './target/*' -not -path './.git/*' -delete
bash scripts/checks.sh
cargo test 2>&1 | tail -5
git status --short
git log --oneline origin/master..HEAD | wc -l | tr -d ' '
```
Expected: 7/7 OK, `test result: ok`, working tree clean, `14` commit ahead.

- [ ] **Step 7: Decisione push (opzionale, richiede autorizzazione utente)**

Pattern STEP19 Phase 6.5.2: **NON pushare** senza esplicito ok dell'utente.
Se l'utente autorizza:
```bash
git push origin master
```

---

## Definition of Done — Sub-phase 7.3

- [ ] `grep -rn 'PHASE7\.2→7\.3' src/` = 0.
- [ ] `bash scripts/checks.sh` = 7/7 OK.
- [ ] `cargo test` = ok (26+ passed; nessun fail).
- [ ] `cargo run --bin diffrun -- tests/testsuite.xml` = 95 MATCH ±1 / 9 DIVERGE / 5 SKIPPED.
- [ ] 5 commit nuovi sul branch master (plan + RED + array-keys + runtime-vars + closure).
- [ ] Diary closure committato in `diary/2026-05-23-rawk-phase7.3-closure.md`.
- [ ] Memory `rawk_phase7_state.md` aggiornata + indice MEMORY.md aggiornato.
- [ ] Nessun `._*` file residuo (`find . -name '._*' -not -path './target/*' -not -path './.git/*'` vuoto).

---

## Self-Review (autore plan, 2026-05-23)

- [x] **Spec coverage**: ogni elemento del design doc §3 sub-phase 7.3 (`arrays`, `fs`, `convfmt`, `ofmt`, helper `eval_array_key`, BRIDGE 7.2→7.3) mappato a uno o più step.
- [x] **Placeholder scan**: nessun TBD/TODO non risolto. Singolo placeholder noto: Task 7.3.3 Step 6 cita "verifica come `-F` viene passato a `EvalContext::new`" — accettabile perché documentato come pattern A/B con decisione esplicita (Opzione B se in dubbio).
- [x] **Type consistency**: `Vec<u8>` usato uniformemente per chiavi array e `fs`/`convfmt`/`ofmt`; `&[u8]` per parametri di funzione (signature `get_array_var/set_array_var/EvalContext::new`); `as_slice()` o deref-coerce sui call site `&key`.
- [x] **BRIDGE accounting**: 6 BRIDGE iniziali → 0 finali (4 in Task 7.3.2 + 2 in Task 7.3.3). Eventuali nuovi BRIDGE 7.2→7.4 / 7.2→7.5 introdotti in 7.3.3 sono documentati come adapter espliciti.
- [x] **Sub-phase boundary**: regex matching, field splitting, builtin printf, output rimangono fuori scope 7.3 — esplicitamente deferred a 7.4–7.6.
- [x] **Pre-flight rituale**: incluso in Task 7.3.0 e ripetuto come gate prima di ogni commit (allineato a `macos_forks_cleanup.md`).
- [x] **DRY**: ogni step contiene codice esatto; nessun "similar to Task N" rimandante.
- [x] **TDD-strict**: Task 7.3.1 introduce RED test prima dell'implementazione (Task 7.3.2). I 3 testcase coprono i 3 invarianti distinti (high-byte uniqueness, NUL embedded, for-in byte length).

---

## Execution Handoff

Plan complete and saved to `diary/2026-05-23-rawk-phase7.3-bytes-plan.md`. Due opzioni di esecuzione:

**1. Subagent-Driven (recommended)** — dispatch fresh subagent per task, review tra task, iterazione veloce.

**2. Inline Execution** — esecuzione nella sessione corrente con `executing-plans`, batch con checkpoint.

Quale approccio?
